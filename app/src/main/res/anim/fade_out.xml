<set xmlns:android="http://schemas.android.com/apk/res/android">
    <alpha
        android:fromAlpha="1.0"
        android:toAlpha="0.0"
        android:duration="100" /> <!-- Duración en milisegundos -->
    <set android:repeatMode="reverse" android:repeatCount="2">
        <alpha
            android:fromAlpha="0.0"
            android:toAlpha="1.0"
            android:duration="200" />
    </set>
</set>
